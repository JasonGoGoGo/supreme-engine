（1）print 输出时print ('小明成绩提升的百分点为 %2.1f' %r)，中间没有逗号。
（2）ASCII编码是二进制码，1个字节，Unicode 国际标准编码，两个字节，UTF-8编码是可变字节长编码，如英文1个字节，汉字三个字节。
（3）Python内置的一种数据类型是列表：list。list是一种有序的集合，可以随时添加和删除其中的元素。
（4）一种有序列表叫元组：tuple。tuple和list非常类似，但是tuple一旦初始化就不能修改。
